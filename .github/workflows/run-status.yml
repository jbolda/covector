on: [pull_request]

jobs:
  covector:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: covector
        uses: ./packages/action
        id: covector
        with:
          command: 'status'
